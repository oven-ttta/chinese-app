# คำแนะนำการแก้ปัญหา "ชื่อ, image, date ไม่ถูกบันทึก"

## 🔍 สาเหตุที่แท้จริง

จากการตรวจสอบโค้ด พบว่า:

### ✅ Frontend (add/page.js) - ถูกต้อง

- ส่งข้อมูล: `contributor`, `date`, `strokeOrderGif`

### ✅ API (api/add/route.js) - ถูกต้อง

- รับข้อมูล: `contributor`, `date`, `strokeOrderGif`
- แปลง `strokeOrderGif` → `image` (URL)
- ส่งไป Google: `contributor`, `image`, `date`

### ❌ ปัญหาอยู่ที่: Google Apps Script

**Google Script ของคุณยังเป็นเวอร์ชันเก่า** ที่รับแค่ 5 columns (char, pinyin, thai, tone, meaning)

---

## 🛠️ วิธีแก้ไข (ทำเพียง 3 ขั้นตอน)

### ขั้นตอนที่ 1: อัปเดต Google Apps Script

1. เปิด: https://script.google.com/
2. เลือกโปรเจคของคุณ
3. **ลบโค้ดเดิมทั้งหมด** แล้ววางโค้ดนี้:

\`\`\`javascript
function doPost(e) {
try {
var sheetId = '19J6lDC5t-T1qvOpyO-3hryhClqszLbxwaAPzuejY-1M';
var spreadsheet = SpreadsheetApp.openById(sheetId);
var sheet = spreadsheet.getSheets()[0];
var data = JSON.parse(e.postData.contents);

    // บันทึก 8 columns
    sheet.appendRow([
      data.char,       // A
      data.pinyin,     // B
      data.thai,       // C
      data.tone,       // D
      data.meaning,    // E
      data.contributor,// F ← สำคัญ!
      data.image,      // G ← สำคัญ!
      data.date        // H ← สำคัญ!
    ]);

    return ContentService.createTextOutput(
      JSON.stringify({ "status": "success", "data": data })
    ).setMimeType(ContentService.MimeType.JSON);

} catch (error) {
return ContentService.createTextOutput(
JSON.stringify({ "status": "error", "message": error.toString() })
).setMimeType(ContentService.MimeType.JSON);
}
}
\`\`\`

### ขั้นตอนที่ 2: Deploy ใหม่ (สำคัญมาก!)

**ห้ามแค่กด Save!** ต้องทำแบบนี้:

1. กดปุ่ม **"Deploy"** (มุมขวาบน)
2. เลือก **"New deployment"**
3. เลือก Type: **"Web app"**
4. ตั้งค่า:
   - Description: `"Version 5 - Fixed 8 columns"`
   - **Execute as: "Me"** ← สำคัญ!
   - **Who has access: "Anyone"** ← สำคัญมาก! (ไม่ใช่ "Anyone with Google Account")
5. กด **"Deploy"**
6. **Copy URL ใหม่** (ขึ้นต้นด้วย `https://script.google.com/macros/s/...`)

### ขั้นตอนที่ 3: อัปเดต URL ในโค้ด

เปิดไฟล์ `src/app/api/add/route.js` บรรทัด 106:

\`\`\`javascript
// เปลี่ยนจาก
const GOOGLE_SCRIPT_URL = 'https://script.google.com/macros/s/AKfycbzqUzv6hfUgEdDvacvKCKOxQ742J01JMnk1YhiN601u2HEOLuEnofyhNToqgXmeZuUvZw/exec';

// เป็น URL ใหม่ที่คุณ Copy มา
const GOOGLE*SCRIPT_URL = 'URL*ใหม่ของคุณ';
\`\`\`

---

## 🧪 วิธีทดสอบ

หลังจากทำทั้ง 3 ขั้นตอน:

1. Restart server: `Ctrl+C` แล้ว `npm run dev`
2. เปิด http://localhost:9999/add
3. กรอกข้อมูลครบ (รวมผู้บันทึก, วันที่, รูป)
4. กด "บันทึก"
5. ดู Terminal จะแสดง:
   \`\`\`
   [Form] Contributor: โอ
   [Form] Date: 2025-12-07
   [Google] Sending to Sheet: { ... }
   \`\`\`
6. เช็ค Google Sheet → จะเห็นข้อมูล columns F, G, H

---

## ❗ สิ่งสำคัญ

**ถ้าคุณไม่ Deploy ใหม่** (ขั้นตอนที่ 2):

- แม้จะแก้โค้ดแล้ว ก็ยังใช้เวอร์ชันเก่าอยู่
- ข้อมูล F, G, H จะไม่ถูกบันทึก

**ต้อง Deploy → New deployment ทุกครั้งที่แก้โค้ด!**

---

## 📊 สรุป

| ส่วน          | สถานะ          | หมายเหตุ                |
| ------------- | -------------- | ----------------------- |
| Frontend      | ✅ ถูกต้อง     | ส่งข้อมูลครบ 8 ช่อง     |
| API Route     | ✅ ถูกต้อง     | แปลงและส่งข้อมูลถูกต้อง |
| Google Script | ❌ **ต้องแก้** | ยังรับแค่ 5 columns     |

**ทำตามขั้นตอนที่ 1-3 แล้วจะแก้ไขได้ 100%!** 🎯
